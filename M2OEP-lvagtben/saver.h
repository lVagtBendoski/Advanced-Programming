//
// Created by bobjo on 10/2/2024.
//
#include "Party.h"
#include "character.h"
#include "equipment.h"
#include "inventory.h"
#include <fstream>
#include <iostream>
#include <vector>
#ifndef SAVER_H
#define SAVER_H
class saver {
    /*
     *ended up being harder than I thought. not worth the points
private:
    std::ifstream fileIn;
    const std::string fileName = "saveFile.csv";
    std::ofstream fileOut;
    std::string saveFileName;
    std::vector<std::string> saveFileNames;
    int checkpointNumber;
    Party playerParty;
public:
    saver();
    saver(std::string sfn, int checkpointNum, Party& playerParty);
    ~saver();
    void setSaveFileName(std::string name);
    void setCheckpointNumber(int num);
    std::string getSaveFileName();
    int getCheckpointNumber();
    bool loadSave(std::string &sfn);
    bool saveProgress();


    */
};
#endif //SAVER_H
